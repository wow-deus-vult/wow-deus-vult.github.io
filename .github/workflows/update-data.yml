name: Update Guild Data
on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: master
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install requests beautifulsoup4
      - name: Run DPS collector
        continue-on-error: true
        run: python fetch_guild_data.py
      - name: Run Heal collector
        continue-on-error: true
        run: python fetch_heal_data.py
      - name: Run Potion collector
        continue-on-error: true
        run: python fetch_potion_data.py
      - name: Run Total Damage collector
        continue-on-error: true
        run: python fetch_total_damage.py
      - name: Run Guild Stats collector
        continue-on-error: true
        run: python fetch_guild_stats.py
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git stash --include-untracked
          git pull --rebase origin master
          git stash pop
          git add data/guild-data.json data/guild-heal.json data/potion-stats.json data/total-damage.json data/guild-stats.json data/pending_total_damage.json data/pending_potion.json data/pending_guild_stats.json data/potion-stats_cache.json data/total-damage_cache.json data/guild-stats_cache.json
          git diff --staged --quiet || git commit -m "Auto-update guild data $(date -u '+%Y-%m-%d')"
          git push origin master
